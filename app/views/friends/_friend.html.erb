<div id="<%= dom_id friend %>" class="container  bg-secondary-subtle text-secondary-emphasis p-3 m-4 lead">
  <p>
    <!-- <strong>First name:</strong> -->
    <h1><%= friend.first_name %> <%= friend.last_name %></h1>
  </p>

  <!-- <p>
    <strong>Last name:</strong>
    <%= friend.last_name %>
  </p> -->

  <!-- <p>
    <strong>Email:</strong>
    <%= friend.email %>
  </p>

  <p>
    <strong>Phone:</strong>
    <%= friend.phone %>
  </p>

  <p>
    <strong>Twitter:</strong>
    @<%= friend.twitter %>
  </p> -->
  <p>
    <%= friend.email %> | <%= friend.phone %> | @<%= friend.twitter %>
  </p>
  <%= link_to 'Edit', edit_friend_path(@friend), class:"btn btn-secondary" %>
  <%= link_to 'Delete', friend, class: "btn btn-danger", data: {turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
  <%= link_to "Back", friends_path, class:"btn btn-dark p-1" %>
  <br>

</div>
